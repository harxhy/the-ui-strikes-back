# UI Copilot – Backend to UI

This project converts backend OpenAPI specs into deterministic UI schemas
that can be rendered into production-ready UIs.

Hackathon project for **The UI Strikes Back**.
